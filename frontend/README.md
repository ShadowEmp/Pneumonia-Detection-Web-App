# PneumoVision Frontend

The futuristic, React-based frontend for the PneumoVision Pneumonia Detection system.

## ⚡ Features
*   **Tech Stack:** React, Vite, Tailwind CSS, Framer Motion.
*   **Visuals:** "Neural Grid" aesthetic, 3D particle effects, glassmorphism.
*   **Interactivity:** Smooth scrolling (Lenis), animated transitions, interactive cursor.
*   **PDF Reports:** Client-side generation of detailed medical reports using `jspdf`.

## 🚀 Getting Started

1.  **Install Dependencies:**
    ```bash
    npm install
    ```

2.  **Run Development Server:**
    ```bash
    npm run dev
    ```

3.  **Build for Production:**
    ```bash
    npm run build
    ```

## 📂 Structure
*   `src/pages`: Main route components (Home, Upload, Analysis, About).
*   `src/components`: Reusable UI elements (NeuralField, Cursor, etc.).
*   `src/assets`: Static assets.
